import { Title } from "@deskpro/app-sdk";
import { Container } from "../common";
import type { FC } from "react";

type Props = {
  //..
};

const LinkContact: FC<Props> = () => {
  return (
    <Container>
      <Title title="Link Contact"/>
    </Container>
  );
};

export { LinkContact };
